{
  "name": "byokit-monorepo",
  "private": true,
  "description": "Bring your own AI plan and devices: accounts, link, decide.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=22.18" },
  "scripts": {
    "build": "tsc -b packages/accounts packages/link packages/decide packages/ui-core",
    "check": "tsc -p tsconfig.json",
    "test": "sh scripts/test.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0"
  }
}
